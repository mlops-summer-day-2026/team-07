# Контекст команды team-07

> Заполните пропуски в начале билда. Этот файл читают все агенты команды — Claude Code,
> Codex, Cursor, opencode, у кого что стоит. `CLAUDE.md` рядом просто ссылается сюда,
> так что источник правды один.
>
> **Агенту:** если пропуски не заполнены, не выдумывай — спроси команду.

## Продукт

Строим **Суммаризатор дня** для **Эда**. Его боль: **Произошло ли что-то важное сегодня к текущему часу**.

Готово — это когда: **Заказчик увидит сводку по дню**.

## Стек

- html
- js
- css

Правило одно: никаких зависимостей, которые ставятся дольше полуминуты. Времени мало.

## Правила

- Всё работает **с ноутбука** — демо смотрят и тестируют именно так
- Крупные кнопки, контраст, читается с трёх метров
- Секреты только из `.env`. В код, коммиты и промпты ключи не попадают
- После каждого изменения — одна строка: что поменялось

## Зоны: кто какие файлы держит

Заполните на старте. Один файл — один хозяин: задачи неизбежно пересекаются, файлы — нет.

| Кто | Файлы | За что отвечает |
| :-- | :-- | :-- |
| [имя] | [файлы] | [ядро продукта] |
| [имя] | [файлы] | [ядро продукта] |
| [имя] | [файлы] | [ядро продукта] |
| [имя] | [файлы] | [ядро продукта] |
| [имя] | [файлы] | [ядро продукта] |
| [имя] | [файлы] | [ядро продукта] |
| | | |

**Агенту:** редактируй только файлы своего хозяина. Если для задачи нужна правка
в чужом файле — не делай её, скажи, что нужно, и кому передать.

## Git

Работаем в `main`, без веток и пул-реквестов — на них нет времени.

```bash
git add -A && git commit -m "что сделал" && git pull --rebase && git push
```

Этот цикл — каждые десять-пятнадцать минут и перед каждым большим промптом.
Конфликт: разрулить маркеры → `git add . && git rebase --continue`.
Запутались — `git rebase --abort` вернёт как было.

Никогда: `push --force`, `reset --hard` без разговора с командой, коммит `.env`.
Если на одном ноутбуке нужны две сессии агента — только через `git worktree add ../team-07-2`,
иначе они перетрут друг друга.

## API

Ключ один — OpenRouter, лежит в `.env` (`OPENROUTER_API_KEY`). Через него и текст,
и картинки.

**Текст** (быстрые модели, актуальные id на openrouter.ai/models):

```js
const r = await fetch("https://openrouter.ai/api/v1/chat/completions", {
  method: "POST",
  headers: { "Authorization": `Bearer ${KEY}`, "Content-Type": "application/json" },
  body: JSON.stringify({
    model: "google/gemini-2.5-flash",
    messages: [{ role: "user", content: "..." }]
  })
});
```

**Картинки** — тот же endpoint, модель с генерацией изображений
(например `google/gemini-2.5-flash-image`), в запрос добавить `modalities: ["image", "text"]`,
картинка приходит в ответе как data-url.

**Голос — бесплатно, без API:** `speechSynthesis.speak(new SpeechSynthesisUtterance("текст"))`
прямо в браузере. Распознавание речи тоже встроено (`webkitSpeechRecognition`).

Правила вау-слоя:

- Один проверенный вызов в самом начале, а не в последние двадцать минут
- Всё, что не обязано генерироваться на сцене — сгенерировать заранее и положить файлом
- Звук в браузере играет только после клика пользователя — вешайте на кнопку
- Ключ в JS статичной страницы виден всем. Здесь это допустимо, но решает команда,
  а не агент молча

## Приёмка

Фичу принимает заказчик: «да» двигает дальше, «нет» экономит время. Спор длиннее
двух минут решает заказчик.

## Секреты

Что бы ведущий ни выдал по ходу дня — держите это в `SECRET.md`, он в `.gitignore`.
В этот файл, README и сообщения коммитов не пишите: репозитории публичные, соседи
подсматривают.

## Идея продукта
Суммаризатор:
- Новостей
- Почты
- Календаря
- Погоды

Собирает за текущую дату все данные. Новости и почту выбирает только важные. Погоду текущую. Событи из календаря тоже указывает.  
